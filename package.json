{
    "scripts": {
        "frontend:install": "cd reactnd-contacts-complete && npm install",
        "backend:install": "cd reactnd-contacts-server && npm install",
        "frontend:deploy": "cd reactnd-contacts-complete && npm build",
        "backend:deploy": "cd reactnd-contacts-server && npm build"
    }
}